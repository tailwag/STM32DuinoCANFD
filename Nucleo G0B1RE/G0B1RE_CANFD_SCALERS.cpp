/*  -------------------------------  *
 *  --  G0B1RE_CANFD_SCALERS.cpp --  *
 *  -------------------------------  */
#ifdef ARDUINO_NUCLEO_G0B1RE
#include "STM32DuinoCANFD.hpp"

 const FDCAN_ScalerStruct FDCANScalers[24] = {
  // prescaler, sync, seg1, seg2 
  {128, 1, 13, 2}, //  31,250 bps = 64MHz/(128*16)
  {120, 1, 13, 2}, //  33,333 bps = 64MHz/(120*16)
  {100, 1, 13, 2}, //  40,000 bps = 64MHz/(100*16)
  {80,  1, 13, 2}, //  50,000 bps = 64MHz/(80*16)
  {64,  1, 13, 2}, //  62,500 bps = 64MHz/(64*16)
  {50,  1, 13, 2}, //  80,000 bps = 64MHz/(50*16)
  {48,  1, 13, 2}, //  83,333 bps = 64MHz/(48*16)
  {40,  1, 13, 2}, // 100,000 bps = 64MHz/(40*16)
  {32,  1, 13, 2}, // 125,000 bps = 64MHz/(32*16)
  {25,  1, 13, 2}, // 160,000 bps = 64MHz/(25*16)
  {20,  1, 13, 2}, // 200,000 bps = 64MHz/(20*16)
  {16,  1, 13, 2}, // 250,000 bps = 64MHz/(16*16)
  {10,  1, 13, 2}, // 400,000 bps = 64MHz/(10*16)
  {16,  1,  3, 4}, // 500,000 bps = 64MHz/(16*8)   ✓ VERIFIED
  { 8,  1,  7, 2}, // 800,000 bps = 64MHz/(8*10)
  { 8,  1,  5, 2}, //1,000,000 bps = 64MHz/(8*8)
  { 8,  1,  3, 2}, //1,333,333 bps = 64MHz/(8*6)
  { 5,  1,  5, 2}, //1,600,000 bps = 64MHz/(5*8)
  { 4,  1,  3, 4}, //2,000,000 bps = 64MHz/(4*8)   ✓ VERIFIED
  { 4,  1,  3, 2}, //2,666,667 bps = 64MHz/(4*6)
  { 2,  1,  3, 4}, //4,000,000 bps = 64MHz/(2*8)
  { 2,  1,  3, 2}, //5,333,333 bps = 64MHz/(2*6)
  { 2,  1,  2, 3}, //5,333,333 bps = 64MHz/(2*6) - alternate
  { 1,  1,  3, 4}, //8,000,000 bps = 64MHz/(1*8)
};
#endif
